{% extends 'base.html.twig' %}

{% block title %}Mis Tareas{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h2>Mis Tareas: ( {{ tasks|length }} Tareas encontradas)</h2> 
    
    {% for message in app.flashes('message') %}
        <div class="message">
            {{ message }}
        </div>
    {% endfor %}
    
    {{ include('includes/task-list.html.twig') }}
    
</div>
{% endblock %}
